import styled from 'styled-components'
import { LinkComponent } from './doms'
import { Colors } from '../../../const/Colors'

export const LinkStyle = styled(LinkComponent)`
  text-decoration: none;
  color: ${Colors.gray};
`
