import styled from 'styled-components'
import { SignOutContainer } from './container'
import { Colors } from '../../const/Colors'

export const SignOutContainerStyle = styled(SignOutContainer)`
  .signOutHeader,
  .signOutSubHeader,
  .signOutLead {
    color: ${Colors.black};
  }
`
